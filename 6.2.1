#include<stdio.h>
#include<stdlib.h>

int** fun(int,int);

int main()
{


    printf("%p",fun(3,4));

    getchar();
    return 0;
}

int** fun(int n,int m)
{
    int**tab = malloc(n*sizeof(int*));

    for(int i=0;i<n;i++)
    {
        tab[i] = malloc(m*sizeof(int));
    }

    return tab;
}
